[node:AVS4000_Complex_Bulkio]

;;;;;; Required configuration

; Override the domain name the node will register with
DOMAIN_NAME=TOA

; Specify which node is being launched
NODE_NAME=AVS4000_Complex_Bulkio
;DCD_FILE=nodes/AVS4000_Complex_Bulkio/DeviceManager.dcd.xml

;;;;;; Optional configuration

; Enable/disable this configuration
;enable=True

; Specify the log level
; TRACE, DEBUG, INFO, WARN, ERROR, FATAL
;DEBUG_LEVEL=INFO

; Specify a URI for a logging configuration
;LOGGING_CONFIG_URI=sca:///nodes/AVS4000_Complex_Bulkio/AVS4000_Complex_Bulkio.log4j

